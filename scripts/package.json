{
  "name": "roamer-device-auth",
  "private": true,
  "type": "module",
  "description": "Dependencies for roamer-device-auth.mjs (AUTH-14 Safari device-code auth path). Not published — installed on demand by roamer-bridge.sh.",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "open": "^11.0.0"
  }
}
