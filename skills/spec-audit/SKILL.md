---
name: spec-audit
description: Audit spec coverage — compare spec items to test coverage and flag gaps
allowed-tools: ["Read", "Glob", "Grep", "mcp__roamer__read_resource_by_uri", "mcp__roamer__list_specs", "mcp__roamer__get_spec_area"]
---

Call `read_resource_by_uri` with uri `roamer://prompt/spec-audit.md` to fetch this workflow's current instructions, then follow them exactly from their own first step. The fetched text is the authoritative source for this skill — do not skip, summarize, or substitute steps from memory of a prior version.
